# .NET build artifacts
bin/
obj/
*.user

# Generated by MSBuild BundleExtension target (rebuilt on every dotnet build)
CourtMetaAPI/wwwroot/extension/
CourtMetaAPI/wwwroot/court-meta-extension.zip

# IDE / tool config
.claude/
.vs/
*.suo

# Local notes
input.txt

# Telemetry log files (rolling per-day NDJSON)
CourtMetaAPI/logs/
logs/

# Extension private key — store in Jenkins as Secret File (ID: court-meta-extension-key)
installer/extension-private-key.pem
